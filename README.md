# shooot
Mobile App to Capture Camera Array Photos

## Client
- `cd` into the `client` directory
- `npm install`
- `npm start`

## Server
- `cd` into the `server` directory
- `npm install`
- `npm start`